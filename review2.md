# Laravel Lesson レビュー②

## Todo編集機能

### @method('PUT')を記述した行に何が出力されているか
<input type="hidden" name="_method" value="PUT">

### findメソッドの引数に指定しているIDは何のIDか
todoのID

### findメソッドで実行しているSQLは何か
select id from todo where id = (探してるレコードのIDの数字)

### findメソッドで取得できる値は何か
todoインスタンス

### saveメソッドは何を基準にINSERTとUPDATEを切り替えているのか
idの値があるかどうか

## Todo論理削除

### traitとclassの違いとは
traitはいくつかのプロパティやメソッドをまとめ、classに追加できるようにしたもの。classと違い、インスタンス化できない。
classはオブジェクトの設計図のようなもの、同じ種類のオブジェクトを作成するためのテンプレートのような機能。traitと違い、インスタンス化できる。

### traitを使用するメリットとは
・コードの再利用が可能。
・メソッドやプロパティをクラスの継承をしなくてもクラス間でコードを共有化できる。

## その他

### TodoControllerクラスのコンストラクタはどのタイミングで実行されるか
コントローラのメソッドを実行するタイミング

### RequestクラスからFormRequestクラスに変更した理由
バリデーションのルールを新たに追加設定するため。

### $errorsのhasメソッドの引数・返り値は何か
引数→入力欄のname属性
返り値→真偽値

### $errorsのfirstメソッドの引数・返り値は何か
引数→入力欄のname属性
返り値→エラー文

### フレームワークとは何か
アプリケーションの基本的な構造部や枠組み、骨組みをまとめたもの。

### MVCはどういったアーキテクチャか
Webサーバーから受け取ったリクエストをWebサーバーに返却するまでの一連の流れに、
以下のようにMVCそれぞれに役割を持たせる特徴があるアーキテクチャ。
・Model→DBとのやり取りを司る
・view→ユーザーの表示させる画面を生成を司る
・Controller→ユーザー空の入力データに基づき、Modelやviewの制御を行う
・Routing→ユーザーからのリクエストによってどの処理を動かすのか判断を行う

### ORMとは何か、またLaravelが使用しているORMは何か
ORMとは、sql文を使用しなくてもデータベース操作ができる機能。
データベースとModelのclassを関連付け、データ操作を行う。
Laraveが使用するORMは「Eloquent」。


### composer.json, composer.lockとは何か
composer.jsson→インストールするパッケージやライブラリを管理するファイル。
composer.lock→インストールしたパッケージやライブラリを管理するファイル。


### composerでインストールしたパッケージ（ライブラリ）はどのディレクトリに格納されるのか
vendorディレクトリ